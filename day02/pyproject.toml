[project]
name = "venv-day04"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sohee0345", email = "xxgml0410@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.1.1",
    "pandas>=3.0.5",
    "seaborn>=0.13.2",
]

[project.scripts]
venv-day04 = "venv_day04:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
